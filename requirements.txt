# contai-analysis — Python dependencies

# Step 1 — build_inventory (and downstream scripts that use DataFrames)
pandas==2.2.2

# Step 1.5 — xml_normalization (file downloads from S3)
requests==2.32.5
tqdm==4.67.3

# Uncomment as pipeline steps are implemented:
# lxml==5.2.2       # XML parsing (steps 2, 3)
# openpyxl==3.1.2   # Excel report export (step 4)
