# Secrets — never commit API keys. (Keys should live in env vars, not files; this is a guard.)
.env
.env.*
*.key
*_secret*
aws_credentials*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local scratch
scratch_*

# Research positioning notebook — raw thinking, rejected hypotheses, alternative
# framings. Not for the public repo; the manuscript is what gets published.
RESEARCH_NOTEBOOK.md

# Tool / local (not project work)
.claude/
.ua/
.agents/
.commandcode/
graphify-out/

# arXiv build artifacts — regenerate with scripts/r10_9_final_pass.py
arxiv_build/
arxiv_submission.tar.gz
